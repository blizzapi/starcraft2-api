import { ProfileUrlLocale } from "../types";

/**
 * List of locales used on StarCraft II website (StarCraft2.com)
 */
export const profileUrlLocales = Object.freeze([
  ...Object.values(ProfileUrlLocale),
]);
